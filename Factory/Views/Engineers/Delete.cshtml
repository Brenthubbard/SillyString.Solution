@{
  Layout = "_Layout";
}

@model Factory.Models.Engineer

<h2>Are you sure you want to delete this Engineer?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)

@if (@Model.JoinEntities.Count == 0)
{
  <p>This Engineer is not Licensed to work on any machines yet</p>
}
else
{
  <h4>Machines the Engineer is licensed to work on:</h4>
  <ul>
    @foreach (var join in Model.JoinEntities)
    {
      @Html.DisplayFor(model => model.Name)
      @using (Html.BeginForm())

      {
        <input type="submit" value="Delete" class="btn btn-primary" />
      }
    }
  </ul>
}


@Html.ActionLink("Back to List", "Index")
<p>@Html.ActionLink("Homepage", "Index", "Home")</p>